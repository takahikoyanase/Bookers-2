<div class="container px-5 ps-sm-0">
    <div class="row">
        <div class="col-md-3">
           <h2>User info</h2>
           <p></p>
           <%= attachment_image_tag user, :profile_image, :fill, 50, 50, fallback: "no_image.jpeg" %>
           <table class="table">
               <tr></tr>
               <tr>
                 <th>name</th>
                 <th><%= user.name %></th>
               </tr>
               <tr>
                 <th>introduction</th>
                 <th><%= user.introduction %></th>
               </tr>
           </table>

               <%= link_to "", edit_user_path(@user), class: "btn btn-outline-secondary btn-block fas fa-user-cog" %>

           <h2>New book</h2>
            <% if book.errors.any? %>
             <%= book.errors.count %>error prohibited this obj from being saved
             <% book.errors.full_messages.each do |message| %>
               <%= message %>
             <% end %>
             <% end %>


            <%= form_with model: book, local:true do |f| %>
             <h4>Title</h4>
               <%= f.text_field :title %>
             <h4>Opinion</h4>
               <%= f.text_area :body %>
               <%= f.submit 'Create Book', class: "btn btn-success btn-sm btn-block create book" %>
               <% end %>
        </div>